import 'package:flutter/material.dart';

class SearchAllStations extends StatelessWidget {
  const SearchAllStations({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
